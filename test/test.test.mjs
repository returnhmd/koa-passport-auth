/* eslint-env mocha */

describe(`Mocha can't work with *.mjs files :(`, () => {
  it(`And I don't want to use babel in dependencies!`, done => {
    done();
  });
  it(`I wait when modules will be add to standard.`, done => {
    done();
  });
  it(`Be sure to cover your code with tests!`, done => {
    done();
  });
});
